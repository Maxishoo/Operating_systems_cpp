{
    "name": "C++ MAI Lecture Examples",
    "dockerFile": "Dockerfile",
    "context": "..",
    "appPort": [],
    "extensions": [
        "ms-vscode.cpptools",
        "vscode.cpptools-extension-pack",
        "ms-vscode.cmake-tools"
    ],
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "postCreateCommand": "apt-get update && apt-get install -y cmake && apt-get install -y strace && apt-get install -y gdb && git config --global --add safe.directory '*'"
}